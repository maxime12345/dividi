<%= simple_form_for(reminder, url:  create_item_outside_reminders_path) do |f| %>
  <%= f.input :ghost_name, label: "Ami hors réseau" %>
  <%= f.input :ghost_item, label: "Nom de l'objet" %>
  <%= f.button :submit, class: "btn btn-primary", value: "Ajouter ce rappel"%>
<% end %>
