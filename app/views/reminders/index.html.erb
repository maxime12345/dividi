<div class="wrapper-dashboard">
  <div class="row">

    <div class="col-xs-6">
      <h2>Items rented or borrow to other persons</h2>

      <% @reminders_others.each do |reminder|%>

        <div class="col-xs-12">
          <%= render 'shared/card_reminder', reminder: reminder %>
        </div>

      <% end %>

      <h2>Items I rented or borrow to other persons</h2>

      <% @my_reminders.each do |reminder|%>

        <div class="col-xs-12">
          <%= render 'shared/card_reminder', reminder: reminder %>
        </div>
      <% end %>


    </div>

    <div class="col-xs-6">

    <h2>Network</h2>

    </div>

  </div>
</div>
