<div class="container items-search">
  <div class="entete">
    <div>
      <h3>
        Les objets de mes amis

      </h3>
    </div>
    <%= link_to 'Ajouter un pense-bête', root_path, class: 'btn btn-primary' %>
  </div>

  <%= form_tag items_path, method: :get, id: 'filter-form' do %>

<!--     Module de recherche caché pour le moment -->
    <!--     <i class="fa fa-search" aria-hidden="true"></i>
    <%#= text_field_tag :query,
      params[:query],
      placeholder: "Chercher un objet dans mon réseau", class: 'form-control'
    %> <br> -->

    <select id="category_search" multiple placeholder="<%= @params_categories.empty? ? "Toutes catégories" : "" %>" name="category_search[]">
      <% @categories.each do |category| %>
        <option value="<%= category.id %>" <%="selected=" if @params_categories.include?(category.id.to_s) %> >
          <%= category.name %>
        </option>
      <% end %>
    </select>

<!-- Module de recherche caché pour le moment
    <select id="verbe_search" multiple placeholder="<%#= @params_verbes.empty? ? "Toutes actions" : "" %>" name="verbe_search[]">
      <%# @verbes.each do |verbe| %>
        <option value="<%#= verbe[0] %>" <%#="selected" if @params_verbes.include?(verbe[0]) %> >
          <%#= verbe[1] %>
        </option>
      <%# end %>
    </select> -->

<!--     <div class="buttons">
        <%#= submit_tag "Rechercher", class: 'btn btn-primary' %>
        <%#= link_to "Remise à zéro", items_path, class: "btn" %>
    </div> -->

    <select id="state_search" multiple placeholder="A paramétrer" name="state_search[]">
      <% @params_states.each do |state| %>
        <option value="<%= state %>" >
          <%= state %>
        </option>
      <% end %>
    </select>

  <% end %>


  <div class="results-items">
    <%= t('.results', count: @items.size) %>
    <hr>
  </div>

  <div class="row">
    <% @items.each do |item| %>
      <div class="col-sm-6 col-md-4 col-lg-3">
        <%= render 'shared/card_item', item: item %>
      </div>
    <% end %>
  </div>

  <br>

</div>
